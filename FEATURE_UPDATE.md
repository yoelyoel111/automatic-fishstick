# עדכון תכונה: רקע למספרים בהגרלת מספרים

## תיאור השינוי
נוספה אפשרות להוסיף רקע צבעוני למספרים בטאב של הגרלת מספרים, עם שליטה מלאה על היקף הרקע.

## מה השתנה?

### 1. ממשק משתמש (index.html)
- נוסף checkbox "רקע למספר" - מאפשר להפעיל/לכבות את הרקע
- נוסף בורר צבע "צבע רקע" - מאפשר לבחור את צבע הרקע של המספר
- **חדש:** נוסף סליידר "היקף רקע" - מאפשר לשלוט על גודל הרקע סביב המספר (0-20)

### 2. תרגומים (js/translations.js)
- `numberBackgroundEnabled` - "רקע למספר" / "Number Background"
- `numberBackgroundColor` - "צבע רקע" / "Background Color"
- **חדש:** `numberBackgroundPadding` - "היקף רקע" / "Background Padding"

### 3. לוגיקה (js/makor-core.js, makor.js)
- הפונקציה `generateNumberedStickers()` עודכנה לקרוא את ערכי הרקע והיקף
- הפונקציה `renderNumberedStickers()` עודכנה להציג רקע עם padding דינמי
- הרקע מוצג עם:
  - צבע לפי בחירת המשתמש
  - **padding דינמי** לפי הסליידר (אנכי: value, אופקי: value * 1.5)
  - borderRadius של 4px (פינות מעוגלות)

### 4. אתחול (js/makor-init.js)
- נוסף event listener לסליידר שמעדכן את התצוגה של הערך בזמן אמת

## איך זה עובד?
1. המשתמש בוחר תמונת מדבקה
2. מגדיר טווח מספרים (התחלה וסיום)
3. מסמן את ה-checkbox "רקע למספר"
4. בוחר צבע רקע מהפלטה
5. **חדש:** מזיז את הסליידר "היקף רקע" כדי לשלוט על גודל הרקע (0-20)
6. לוחץ על "צור מדבקות ממוספרות"
7. המספרים מוצגים עם רקע צבעוני שגודלו מתאים בדיוק לבחירה

## יתרונות
- הרקע מתאים אוטומטית לגודל המספר (1-3 ספרות)
- ניתן לבחור כל צבע רקע
- **שליטה מלאה על היקף הרקע** - מ-0 (צמוד למספר) עד 20 (רקע גדול)
- האפשרות אופציונלית - ניתן להשאיר ללא רקע
- הרקע נשמר בהורדת PDF/תמונה
- **תצוגה בזמן אמת** של ערך ההיקף בסליידר

## פרטים טכניים
- הסליידר בטווח 0-20 עם צעדים של 1
- ערך ברירת מחדל: 6
- Padding אנכי = ערך הסליידר
- Padding אופקי = ערך הסליידר × 1.5 (כדי לתת מרווח נוח יותר לספרות)

## קבצים ששונו
- `index.html` - הוספת אלמנטי UI (סליידר)
- `js/translations.js` - הוספת תרגומים
- `js/makor-core.js` - עדכון לוגיקה
- `makor.js` - עדכון לוגיקה (גרסה נוספת)
- `js/makor-init.js` - הוספת event listener לסליידר
