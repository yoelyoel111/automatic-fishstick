# עדכון תכונה: רקע למספרים בהגרלת מספרים

## תיאור השינוי
נוספה אפשרות להוסיף רקע צבעוני למספרים בטאב של הגרלת מספרים.

## מה השתנה?

### 1. ממשק משתמש (index.html)
- נוסף checkbox "רקע למספר" - מאפשר להפעיל/לכבות את הרקע
- נוסף בורר צבע "צבע רקע" - מאפשר לבחור את צבע הרקע של המספר

### 2. תרגומים (js/translations.js)
- `numberBackgroundEnabled` - "רקע למספר" / "Number Background"
- `numberBackgroundColor` - "צבע רקע" / "Background Color"

### 3. לוגיקה (js/makor-core.js, makor.js)
- הפונקציה `generateNumberedStickers()` עודכנה לקרוא את ערכי הרקע
- הפונקציה `renderNumberedStickers()` עודכנה להציג רקע כאשר מופעל
- הרקע מוצג עם:
  - צבע לפי בחירת המשתמש
  - padding דינמי (6px אנכי, 9px אופקי)
  - borderRadius של 4px (פינות מעוגלות)
  - **תיקון ייצוא:** הרקע עוקב בדיוק אחרי המספר בייצוא לתמונה/PDF
    - `display: inline-block` - הרקע מתכווץ לגודל התוכן
    - `whiteSpace: nowrap` - מונע שבירת שורות
    - `lineHeight: 1.2` - גובה שורה מדויק
    - `boxSizing: border-box` - חישוב מדויק של הגבולות

## איך זה עובד?
1. המשתמש בוחר תמונת מדבקה
2. מגדיר טווח מספרים (התחלה וסיום)
3. **חדש:** מסמן את ה-checkbox "רקע למספר"
4. **חדש:** בוחר צבע רקע מהפלטה
5. לוחץ על "צור מדבקות ממוספרות"
6. המספרים מוצגים עם רקע צבעוני שמתאים בדיוק לגודל המספר

## יתרונות
- הרקע מתאים אוטומטית לגודל המספר (1-3 ספרות)
- ניתן לבחור כל צבע רקע
- האפשרות אופציונלית - ניתן להשאיר ללא רקע
- **הרקע נשמר בדיוק בהורדת PDF/תמונה** - תוקן!
- הרקע עוקב אחרי המספר גם בייצוא באיכות גבוהה

## תיקון בעיית הייצוא
הבעיה המקורית הייתה שהרקע "בורח" בייצוא לתמונה. התיקון כלל:
- שימוש ב-`inline-block` במקום `block` - הרקע מתכווץ לגודל התוכן
- הוספת `whiteSpace: nowrap` - מונע שבירת שורות
- הוספת `lineHeight: 1.2` - גובה שורה מדויק
- הוספת `boxSizing: border-box` - חישוב מדויק של padding

## קבצים ששונו
- `index.html` - הוספת אלמנטי UI
- `js/translations.js` - הוספת תרגומים
- `js/makor-core.js` - עדכון לוגיקה + תיקון ייצוא
- `makor.js` - עדכון לוגיקה + תיקון ייצוא (גרסה נוספת)
